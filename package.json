{
  "name": "voxer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outdir public --target browser --minify",
    "dev": "bun run build && bun --watch server/index.ts",
    "start": "bun run build && bun server/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0"
  }
}
